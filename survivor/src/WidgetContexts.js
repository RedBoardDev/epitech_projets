import { createContext } from "react"

const WeatherWidget = createContext();
const NewsWidget = createContext();
const TodoWidget = createContext();

export { WeatherWidget, NewsWidget, TodoWidget };
